# Knotwork

Progress tracking made faster, simpler, and better.

## Usage

### MCP Server

**Command:** `docker compose --project-directory /abs/path/to/repo -f /abs/path/to/compose.mcp.yaml run --rm -T mcp`
